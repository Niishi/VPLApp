var x = 100;

setup = function(){
  var canvas = createCanvas(700, 300);
  canvas.parent('sketch');
};

draw = function(){
  var x = 1 + 2 * 3;
  if (x > 10) {
    rect(199, 200, 200, 300);
  }
  this.rect(,,,);
};
