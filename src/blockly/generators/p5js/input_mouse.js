Blockly.p5js["mouseX"] = function(block) {
	// TODO: Assemble p5js into code variable.
	var code = "";
	code += "mouseX";

	// TODO: Change ORDER_ATOMIC to the correct strength.
	return [code, Blockly.p5js.ORDER_ATOMIC];
};


Blockly.p5js["mouseY"] = function(block) {
	// TODO: Assemble p5js into code variable.
	var code = "";
	code += "mouseY";

	// TODO: Change ORDER_ATOMIC to the correct strength.
	return [code, Blockly.p5js.ORDER_ATOMIC];
};
